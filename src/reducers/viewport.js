// @flow

import Act from '../actions/Types';

const InitialState = {
};

export default function viewporthReducer(state = InitialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

